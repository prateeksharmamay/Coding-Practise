package linkedList.DoublyLinkedList;

public class DoublyLinkedListImpl {
	ListNode head;
	ListNode tail;
	int len;
	
	public DoublyLinkedListImpl() {
		head = null;
		tail = null;
		len = 0;
	}
	
	void insertInFront(ListNode temp){
		
		if(head == null){
			head = temp;
			tail = temp;
		}
		
		else{
			
		}
		
	}
}